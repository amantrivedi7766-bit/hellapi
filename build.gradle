plugins {
    id 'fabric-loom' version '1.8.13'
    id 'com.gradleup.shadow' version '8.3.5'
    id 'maven-publish'
}

group = 'com.hellapi'
version = '0.2.1'

repositories {
    mavenCentral()
    maven { url 'https://maven.fabricmc.net/' }
}

dependencies {
    minecraft "com.mojang:minecraft:${project.minecraft_version}"
    mappings "net.fabricmc:yarn:${project.yarn_mappings}:v2"
    modImplementation "net.fabricmc:fabric-loader:${project.loader_version}"
    modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"

    // Runtime math module intentionally bundled for advanced smoothing and 1MB+ practical artifact size.
    implementation 'org.apache.commons:commons-math3:3.6.1'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
    withSourcesJar()
}

tasks.withType(JavaCompile).configureEach {
    options.release = 21
}

tasks.shadowJar {
    archiveClassifier = 'dev-all'
    zip64 = true

    // Keep shaded archive minimal to avoid huge entry counts from unrelated classpaths.
    dependencies {
        include(dependency('org.apache.commons:commons-math3:3.6.1'))
    }
}

tasks.remapJar {
    dependsOn tasks.shadowJar
    inputFile = tasks.shadowJar.archiveFile
}
